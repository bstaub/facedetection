import React from 'react';

const Navigation = props => {
  return (
    <nav className="navigation" style={{display: 'flex', flexDirection}}>
      Sign-In
    </nav>
    );
};

export default Navigation;
