import React from 'react';

const Navigation = props => {
  return (
    <nav className="navigation" style={{display: 'flex', flexDirection: 'flex'}}>
      Sign-In
    </nav>
    );
};

export default Navigation;
