import React from 'react';

const Navigation = props => {
  return (
    <nav
      className="navigation"
      style={{ display: 'flex', Jus 'flex' }}
    >
      Sign-In
    </nav>
  );
};

export default Navigation;
